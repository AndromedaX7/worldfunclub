<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View" />

        <variable
            name="data"
            type="com.ds.worldfunclub.responsebean.ExpressBean.DataBean.DeliveryListBean" />

        <variable
            name="pos"
            type="int" />

        <variable
            name="max"
            type="int" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="40dp"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:orientation="vertical">

            <View
                android:layout_width="2dp"
                android:layout_height="5dp"
                android:background="#FF777777"
                android:visibility="@{pos ==0 ?View.INVISIBLE:View.VISIBLE}" />

            <View
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:background="@{pos==0?@drawable/express_0:@drawable/express_1}" />

            <View
                android:layout_width="2dp"
                android:layout_height="wrap_content"
                android:background="#FF777777"
                android:visibility="@{pos ==max ?View.INVISIBLE:View.VISIBLE}" />
        </LinearLayout>

        <TextView
            selectView="@{pos ==0}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="48dp"
            android:text='@{"["+data.operator+"]"+data.context+"\n"+data.time}'
            android:textColor="@color/selector_express_color" />


    </LinearLayout>
</layout>