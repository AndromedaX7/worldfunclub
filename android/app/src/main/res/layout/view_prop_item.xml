<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="selected"
            type="int" />

        <variable
            name="pos"
            type="int" />

        <variable
            name="vm"
            type="com.ds.worldfunclub.ui.delegate.GoodsPropValueDelegate" />

        <variable
            name="data"
            type="com.ds.worldfunclub.responsebean.GoodsDetailsResp2.DataBean.SpecAttrBean.SpecItemsBean" />
    </data>

    <LinearLayout
        android:layout_width="wrap_content" android:layout_marginBottom="6dp"
        android:layout_height="wrap_content"
        android:onClick="@{()->vm.onSelected(pos)}"
        android:orientation="vertical"
        android:paddingEnd="14dp">

        <TextView
            selectView="@{selected ==pos }"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:paddingStart="14dp" android:paddingEnd="14dp" android:paddingTop="6dp" android:paddingBottom="6dp"
            android:background="@drawable/selector_prop_item"
            android:gravity="center"
            android:text="@{data.spec_value}"
            android:textColor="@color/selector_color_prop_item" />

    </LinearLayout>
</layout>